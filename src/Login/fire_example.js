import firebase from 'firebase';

// Your web app's Firebase configuration
var firebaseConfig = {
    /*
    Replace it with your firebaseConfig.
    */
};
// Initialize Firebase
const fire = firebase.initializeApp(firebaseConfig);

export default fire;